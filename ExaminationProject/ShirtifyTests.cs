using Moq;
using ExaminationProject.ViewModel;
using ExaminationProject.Services;
using Microsoft.Maui.Controls;
using Xunit;

namespace ExaminationProject.Tests
{
    public class MainPageTests
    {
        //[Fact]
        //public void MainPage_BindsToViewModel()
        //{
        //    var mockPhotoService = new Mock<PhotoService>();
        //    var mockNavigation = new Mock<INavigation>();

        //    var viewModel = new MainViewModel(mockPhotoService.Object);

        //    var mainPage = new MainPage(viewModel, mockNavigation.Object);

        //    var bindingContext = mainPage.BindingContext;

        //    Assert.Equal(viewModel, bindingContext);
        //}
    }
}
